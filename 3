#include <stdio.h>

int mian()
{
  int a=0,b=0,c=0,sum=0;
  float average=0;
  printf("请输入三个整数：\n");
  scanf("%d %d %d",&a,&b,&c);
  sum=a+b+c;
  average=sum/3.0;
  printf("三个数的和为：%d\n",sum)；
  printf("三个数的平均值为：%f\n",average);
  return 0;
}
