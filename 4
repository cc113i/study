#include <stdio.h>

int main()
{
   char c1, c2;   //定义字符变量
   printf("请输入两个字符：\n");
   c1=getchar();    //读取字符
   c2=getchar();
   printf("输出两个字符：\n");
   putchar(c1);   //输出字符
   putchar(c2);
   printf("\n");
   return 0;
}
